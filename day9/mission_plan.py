# -*- coding: utf-8 -*-
"""
任务安排
m个存储空间的单进程机器
现在又n个任务，计算需要R[i]个空间，存储需要O[i]个空间,O[i] < R][i]
问如何安排这n个请求的顺序

算法:
    第k个任务的计算占用空间 + 前面k-1个任务的存储占用空间 最小
    得到:
        O[1] + O[2] + ...O[j] + ... + O[K-1] + R[K]  正常理想顺序
        O[1] + O[2] + ...O[K] + ... + O[K-1] + R[j]  假如第K个任务和之前某个j任务交换了的结果
    ==> line1 <= line2
    ==> O[j] + R[K] <= O[K] + R[j]
    ==> R[K]-O[K] <= R[j] - O[j]
    得到将任务按照R[i]-O[i]的值从大到小排列即可 贪心法

代码实现：略
"""
__author__ = 'guyu'
